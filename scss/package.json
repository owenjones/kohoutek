{
  "dependencies": {
    "uikit": "^3.15.10"
  },
  "devDependencies": {
    "css-minify": "^2.0.0"
  },
  "scripts": {
    "compile": "sass --no-source-map main.scss main.css",
    "minify": "css-minify -f main.css -o ../splash/html/static -o ../portal/src/static/css",
    "build": "npm run compile && npm run minify"
  }
}
